import { type FC } from 'react';

import { AddressTable } from '@/widgets/address/table';

export const AddressesPage: FC = () => {
  return (
    <AddressTable />
  );
};

export default AddressesPage;
